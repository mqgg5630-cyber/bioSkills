# 科研版本管理与工具全家桶总结指南

## 一、 Git 核心概念：科研人员的“时光机”

在科研中，版本管理不仅是备份代码，更是确保**实验可重复性**的关键。

### 1. 三大核心术语
*   **Commit (提交)**: 代码的“快照”。每一次 commit 都是一个存档点，建议每完成一个功能或修复一个 bug 就提交一次。
*   **Branch (分支)**: 实验的“平行世界”。用于隔离不同的尝试（如：`main` 跑基础模型，`exp-deep-learning` 试深度学习）。
*   **Tag (标签)**: 实验的“里程碑”。专门标记投稿版本、接收版本或生成论文图表的特定时刻（如 `v1.0-submission`）。

---

## 二、 科研版本管理实战策略

### 1. 分支管理建议
*   **`main` 分支**: 存放最稳定的代码，保证随时能跑通。
*   **功能/实验分支 (`feat/` 或 `exp/`)**: 每当有一个新想法（如加入新特征），开个新分支。
*   **合并与舍弃**: 实验成功的合并回 `main`，失败的直接删除或保留作为记录，不污染主线。

### 2. 标签的使用（极其重要）
*   **投稿节点**: `git tag -a v1.0-submission -m "Submitted to Nature"`
*   **修改节点**: `git tag -a v1.1-revision -m "Major revision changes"`
*   **意义**: 无论代码后续如何演进，通过 `git checkout v1.0-submission` 即可瞬间找回投稿时的环境。

---

## 三、 误删与恢复：后悔药方案

*   **云端备份**: 只要执行了 `git push`，GitHub 就是你的永恒备份。
*   **整个项目丢失**: `git clone <url>`
*   **文件误删（.git还在）**: `git restore .` 或 `git checkout -- .`
*   **历史回溯**: `git log` 查看 ID，然后 `git checkout <commit-id>`。

---

## 四、 大文件管理：科研数据的容身之所

Git 不擅长存大数据（>100MB），推荐以下方案：
1.  **Hugging Face**: 机器学习领域的“GitHub”，支持无限大的模型权重和数据集，学术友好，完全免费。
2.  **DVC (Data Version Control)**: “数据版的 Git”。代码存 GitHub，数据存服务器/网盘，Git 只记录数据的指纹。
3.  **Git LFS**: Git 官方插件，适合存储 1GB 以内的大文件。

---

## 五、 现代科研全家桶工具链（超级推荐）

### 1. Lazygit: 效率神器
*   **定位**: Git 的终端图形化界面。
*   **优势**: 告别复杂的命令行，通过空格、`c` (commit)、`p` (push) 即可极速管理代码。

### 2. Jupytext: Jupyter 的救星
*   **定位**: 将 `.ipynb` 自动同步为 `.py`。
*   **优势**: 让 Jupyter Notebook 的改动在 Git 中清晰可见，不再有乱码冲突。

### 3. Weights & Biases (W&B): 实验黑匣子
*   **定位**: 实验记录平台。
*   **优势**: 自动记录每一版代码对应的**超参数、准确率曲线、损失函数图表**。它是连接“代码”与“实验结果”的桥梁。

---

## 六、 案例：抗菌肽 (AMP) 预测项目工作流

1.  **环境配置**:
    *   开启 Jupytext 同步。
    *   初始化 DVC：`dvc init`。
    *   连接 W&B：`wandb.init()`。

2.  **日常开发**:
    *   在 Jupyter 中写代码，保存时 Jupytext 自动生成 `.py`。
    *   如有新数据，使用 `dvc add data/amp.csv`。

3.  **提交与同步**:
    *   打开 `lazygit`，选中改动的文件和 `.dvc` 指针。
    *   提交并推送：`c` -> `P`。

4.  **成果发表**:
    *   打上标签：`v1.0-published`。
    *   将模型权重上传至 **Hugging Face**。

---

## 七、 总结建议
**“勤 Commit，多 Push，必打 Tag，数据靠 DVC/HuggingFace。”**
这套方案能确保你的科研工作既高效又安全，彻底告别“代码找不着”和“实验无法复现”的焦虑。
