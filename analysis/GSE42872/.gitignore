# ---------------------------------------------------------------------------
# 运行产物一律不入库。
# 理由：figures/ 和 results/ 每跑一次都会重新生成（PNG 字节级都不同），
# 若被 git 跟踪，你本地跑一次就与远端冲突，git pull 会直接 Aborting。
# 想看样例结果去 docs/showcase/（那是固定快照，不会被脚本覆盖）。
# ---------------------------------------------------------------------------
data/
figures/
results/
